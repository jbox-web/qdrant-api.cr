/lib/
/.shards/
/bin/
.DS_Store
/shard.lock
*.dwarf
.spectator-failures

# Generator jar — the generator comes from Homebrew (local) or Maven Central (CI);
# this only guards against a stray jar being dropped in the working copy.
/openapi-generator-cli*.jar

# `crystal docs` output (published to Pages by CI) and internal plan docs
/docs/
